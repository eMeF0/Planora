@page "/notes/create/{Id:guid}"
@using Planora.Web.Models.DTOs.Note
@inject NavigationManager NavigationManager
@inject HttpClient Http

<section class="form-layout">
    <div class="form-intro">
        <span class="section-kicker">Note</span>
        <h2>Add project context</h2>
        <p>Keep decisions, meeting notes, and important context visible beside the work.</p>
    </div>

    <EditForm Model="newNote" OnValidSubmit="CreateNewNote" class="form-card">
        <DataAnnotationsValidator />
        <div class="form-grid">
            <label class="input-control">
                <span>Note content</span>
                <InputTextArea @bind-Value="newNote.Content" placeholder="Decision, discussion summary, or reminder" rows="7" />
                <ValidationMessage For="@(() => newNote.Content)" />
            </label>
        </div>

        <div class="form-actions">
            <a class="btn btn-secondary" href="@($"/projects/{Id}")">Cancel</a>
            <button class="btn btn-primary" type="submit">Save note</button>
        </div>
    </EditForm>
</section>

@code {
    [Parameter]
    public Guid Id { get; set; }

    private CreateNoteDto newNote = new();

    private async Task CreateNewNote()
    {
        var response = await Http.PostAsJsonAsync($"api/note/{Id}", newNote);

        if (response.IsSuccessStatusCode)
        {
            NavigationManager.NavigateTo($"/projects/{Id}");
        }
    }
}
